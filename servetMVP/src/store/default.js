import httpClient from "../httpClient";
import config from "../config";

const state = {}
const getters = {}
const actions = {}
const mutations = {}

export default {
    state, getters, actions, mutations
}
